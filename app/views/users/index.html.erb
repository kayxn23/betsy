
<h1>Merchants</h1>
<ul>
  <!-- Need help getting this to show, is not working -->
  <!-- Says merchants is not nil so why is this not working? -->
  <!-- Says it's an active record relation instead of a collection of merchants? -->  <% @merchants.each do |merchant| %>
    <li>
      <h3><%= link_to "#{merchant.name}", merchant_products_path(merchant.id)  %></h3>
    </li>

    <% end %>

</ul>
